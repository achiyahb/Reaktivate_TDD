{
  "name": "reaktivate-tdd-challenge-1-sample-begin",
  "version": "1.0.0",
  "description": "How we create a fast-test architecture from scratch.",
  "keywords": [],
  "main": "src/index.tsx",
  "dependencies": {
    "mobx": "6.3.10",
    "mobx-react": "7.2.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "^5.0.1"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/react": "^17.0.87",
    "@types/react-dom": "^17.0.26",
    "typescript": "^4.9.5"
  },
  "scripts": {
    "start": "SKIP_PREFLIGHT_CHECK=true TSC_COMPILE_ON_ERROR=true NODE_OPTIONS='--openssl-legacy-provider' react-scripts start",
    "build": "SKIP_PREFLIGHT_CHECK=true TSC_COMPILE_ON_ERROR=true NODE_OPTIONS='--openssl-legacy-provider' react-scripts build",
    "test": "SKIP_PREFLIGHT_CHECK=true TSC_COMPILE_ON_ERROR=true NODE_OPTIONS='--openssl-legacy-provider' react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
